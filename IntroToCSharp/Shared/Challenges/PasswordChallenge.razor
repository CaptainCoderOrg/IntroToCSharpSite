<Challenge Title="Password Challenge" Gold="175" XP="25">
    <Text>
        Write a program that prompts the user to enter a password.
        If they are successful,
        the program displays, "Access Granted!". If they are are not successful, the program
        displays, "Incorrect. You have {N} tries remaining". If they run out tries, the program
        displays, "Access denied!" and exits.
    </Text>
        <Text>
            <TaskList Name="Perimeter of a Rectangle">
                <TaskItem Name="Create a new project">If you have not already done so, create a new C# project called "Password Checker"</TaskItem>
                <TaskItem Name="Display Rectangle"> Display, "Enter the password: "</TaskItem>
                <TaskItem Name="Prompt the User"> Prompt the user to enter a string.</TaskItem>
                <TaskItem Name="Read User Input"> Read user input and store the value in a variable.</TaskItem>
                <TaskItem Name="Invalid inputs"> Check if the password is correct.</TaskItem>
                <TaskItem Name="Declare i"> If correct, display "Access Granted".</TaskItem>
                <TaskItem Name="Write Loop"> If incorrect, display "Incorrect. You have {N} tries remaning."</TaskItem>
                <TaskItem Name="Within the Loop"> If the user is out of attempts, display "Access denied!"</TaskItem>
            </TaskList>
            <Question Text="Generate a share link of your Program.cs file and put it here"/>
        </Text>
</Challenge>