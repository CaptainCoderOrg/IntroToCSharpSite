<code class="code-tag" style="@Style">@ChildContent</code>
@code
{
    [Parameter]
    public RenderFragment ChildContent { get; set; } = null!;
    private string Background = "background: var(--mud-palette-background-grey);";
    private string Color = "color: var(--mud-palette-gray-default);";
    private string Style => $"{Background} {Color}";
}