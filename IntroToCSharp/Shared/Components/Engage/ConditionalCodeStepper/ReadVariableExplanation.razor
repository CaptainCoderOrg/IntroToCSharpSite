The computer reads the <b>value</b> stored in the <b>memory address</b>
labeled <Code>@Name</Code> and substitutes it into the <b>expression</b>.
@code {
    [Parameter, EditorRequired]
    public string Name {get; set;} = string.Empty;
}