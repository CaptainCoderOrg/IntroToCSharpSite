The <b>equality operator</b> (<Code>==</Code>) checks if <Code>@LeftValue</Code> is the same
<b>value</b> as <Code>@RightValue</Code> and substitutes the resulting
<b>bool</b> into the <b>expression</b>.
@code {
    [Parameter, EditorRequired]
    public string LeftValue {get;set;} = string.Empty;

    [Parameter, EditorRequired]
    public string RightValue {get;set;} = string.Empty;
}