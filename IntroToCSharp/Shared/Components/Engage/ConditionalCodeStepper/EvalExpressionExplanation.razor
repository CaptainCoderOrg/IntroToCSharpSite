Before the computer can execute this line of code, it must first <b>evaluate</b>
the <b>expression</b> <Code>@Expr</Code>.
@code {
    [Parameter, EditorRequired]
    public string Expr { get; set; } = null!;
}