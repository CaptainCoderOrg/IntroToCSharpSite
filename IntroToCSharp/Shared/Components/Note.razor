<MudPaper Elevation="25" Class="@Class"><b>Note: </b>@ChildContent</MudPaper>

@code
{
    [Parameter]
    public string Class {get; set;} = "pa-2";
    [Parameter]
    public RenderFragment ChildContent {get; set;} = null!;
}

@* we think it might be helpful to add bottom and top margins to the note component so that it is spaced out more and easier to read *@